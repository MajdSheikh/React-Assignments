import React from 'react'

const Component3 = (props) => {
    return (
    <div>
        <h1 style={{color: props.color1, backgroundColor: props.color2}}>{props.text}</h1>
    </div>
    )
}

export default Component3

